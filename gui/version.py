"""Single source of truth for the GUI's own version, checked against
GitHub releases by release_check.py. Bump this on every tagged release -
it is what gets compared against the repo's latest release tag."""

__version__ = "0.1.0"
